const $=(q)=>document.querySelector(q);
const api=async(path,options={})=>{const r=await fetch(path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const data=await r.json();if(!r.ok)throw new Error(data.error||'Erro');return data};
let dash=null,progress=null,current=null,startedAt=0,mediaRecorder=null,audioChunks=[],recordingUrl=null;

async function refresh(){[dash,progress]=await Promise.all([api('/api/dashboard'),api('/api/progress?days=30')]);renderDashboard();renderProgress()}
function renderDashboard(){
  $('#placementBanner').classList.toggle('hidden',dash.placement.completed);
  const pressure=dash.reviewPressure?.freezeNew?' Há muitas revisões antigas: conteúdo novo foi temporariamente reduzido para recuperar retenção.':'';
  $('#heroText').textContent=dash.placement.completed?`Nível estimado ${dash.placement.level}. Há ${dash.dueTotal} revisões/itens vencidos.${pressure}`:'Comece pelo diagnóstico rápido. Depois o sistema mistura revisão, gramática, escuta, leitura, chunks e contraste com o português conforme seu progresso.';
  const stats=[['XP',dash.total.xp],['Nível',dash.total.level],['Sequência',`${dash.total.streak} dia(s)`],['Precisão',`${dash.total.correctRate}%`],['Pendentes',dash.dueTotal],['Erros abertos',dash.insights.openErrors]];
  $('#stats').innerHTML=stats.map(([l,v])=>`<div class="stat"><b>${v}</b><span>${l}</span></div>`).join('');
  $('#achievements').innerHTML=dash.achievements.map(a=>`<span title="${safe(a.description)}">${a.unlocked?'🏆':'🔒'} ${safe(a.title)}</span>`).join('');
  $('#adaptiveFocus').innerHTML=dash.insights.attention?.length?`<p class="sub">Prioridade calculada com domínio, erros abertos, erros recentes e tendência:</p><div class="principles">${dash.insights.attention.slice(0,5).map(x=>`<span>${safe(x.title)} · atenção ${x.attentionScore}/100</span>`).join('')}</div>`:'<p class="sub">Pratique algumas atividades para o sistema identificar padrões confiáveis.</p>';
  $('#quest').innerHTML=dash.quest.rows.map(x=>`<div class="questRow"><div><b>${label(x.key)}</b><div class="bar"><i style="width:${Math.round(x.value/x.target*100)}%"></i></div></div><span>${x.value}/${x.target}</span></div>`).join('')+`<p class="footerNote">${dash.quest.percent}% da missão concluída. A sequência conta consistência, não perfeição.</p>`;
  $('#variantSelect').value=dash.preferences.spanishVariant||'es';$('#variantSelect').onchange=async e=>{await api('/api/preferences',{method:'POST',body:JSON.stringify({spanishVariant:e.target.value})});await refresh()};
}
function renderProgress(){
  const s=progress.summary,t=progress.trend;
  $('#progressStats').innerHTML=[['Atividades',s.attempts],['Precisão 30d',`${s.accuracy}%`],['Dias ativos',s.activeDays],['Memórias maduras',s.mature],['Retenção estimada',s.averageRetrievability==null?'—':`${s.averageRetrievability}%`],['Atrasadas +7d',s.overdue7]].map(([l,v])=>`<div class="miniStat"><b>${v}</b><span>${l}</span></div>`).join('');
  const delta=t.accuracyDelta;const trendText=delta==null?'Tendência ainda sem amostra':delta>3?`↑ precisão +${delta} p.p.`:delta<-3?`↓ precisão ${delta} p.p.`:'→ precisão estável';
  $('#trendBadge').textContent=trendText;$('#trendBadge').className=`trendBadge ${delta==null?'neutral':delta>3?'up':delta<-3?'down':'neutral'}`;
  $('#attentionList').innerHTML=progress.attention.length?progress.attention.map(x=>`<article class="attentionItem ${safe(x.tier)}"><div class="attentionScore"><b>${x.attentionScore}</b><small>/100</small></div><div class="attentionBody"><div class="attentionHead"><h3>${safe(x.title)}</h3><span>${safe(x.tier==='urgent'?'Prioridade alta':x.tier==='focus'?'Foco recomendado':x.tier==='watch'?'Observar':'Estável')}</span></div><p><b>Por que apareceu:</b> ${safe(x.reasons.join(' · '))}</p><p>${safe(x.explanation)}</p><p class="recommend"><b>Próxima ação:</b> ${safe(x.recommendation)}</p></div><button class="ghost" data-focus-type="${safe(x.skillType)}" data-focus-key="${safe(x.skillKey)}">Treinar</button></article>`).join(''):'<p class="sub">Ainda não há dados suficientes para calcular prioridades confiáveis.</p>';
  const rows=progress.activity.slice(-30),maxXp=Math.max(1,...rows.map(x=>x.xp));
  $('#activityChart').innerHTML=rows.length?rows.map(x=>`<div class="activityDay" title="${safe(x.day)} · ${x.xp} XP · ${x.accuracy}%"><i style="height:${Math.max(5,Math.round(x.xp/maxXp*100))}%"></i><small>${safe(x.day.slice(8))}</small></div>`).join(''):'<p class="sub">Sem atividade neste período.</p>';
  $('#activityNote').textContent=`Últimos 7 dias: ${t.recent.attempts} atividades, ${t.recent.accuracy}% de precisão. Período anterior: ${t.previous.attempts} atividades, ${t.previous.accuracy}% de precisão.`;
  $('#modePerformance').innerHTML=progress.modes.length?progress.modes.map(x=>`<div class="metricRow"><div><b>${label(x.mode)}</b><span>${x.attempts} tentativas</span></div><div class="metricBar"><i style="width:${x.accuracy}%"></i></div><strong>${x.accuracy}%</strong></div>`).join(''):'<p class="sub">Ainda sem dados por habilidade.</p>';
  const sr=progress.srs;$('#srsHealth').innerHTML=`<div class="healthGrid"><span><b>${sr.new}</b> novos</span><span><b>${sr.learning}</b> aprendendo</span><span><b>${sr.review}</b> revisão</span><span><b>${sr.relearning}</b> reaprendendo</span></div><p class="footerNote">${sr.due} item(ns) vencido(s); ${sr.overdue7} estão atrasados há mais de 7 dias. Quando a dívida de revisão fica alta, o SIDES reduz itens novos.</p>`;
  $('#errorBreakdown').innerHTML=progress.errorBreakdown.length?progress.errorBreakdown.map(x=>`<div class="errorRow"><span>${safe(errorLabel(x.kind))}</span><b>${x.count}</b></div>`).join(''):'<p class="sub">Nenhum padrão de erro registrado no período.</p>';
}
function label(k){return {reviews:'Revisões',vocabulary:'Vocabulário',grammar:'Gramática',listening:'Escuta',reading:'Leitura',speaking:'Fala',chunk:'Chunks',contrast:'PT × ES'}[k]||k}
function errorLabel(k){return {wrong:'Resposta incorreta',accent:'Acentuação/grafia',empty:'Sem resposta',comprehension:'Compreensão',self_assessed:'Autoavaliação',self_assessed_review:'Revisar fala'}[k]||k.replaceAll('_',' ')}
function showPractice(mode,title){$('#dashboardView').classList.add('hidden');$('#practiceView').classList.remove('hidden');$('#modeLabel').textContent='Treino';$('#practiceTitle').textContent=title;$('#xpToast').textContent='';$('#practiceCard').innerHTML='<p>Carregando...</p>';startedAt=performance.now();return mode}
function back(){stopRecording();$('#practiceView').classList.add('hidden');$('#dashboardView').classList.remove('hidden');refresh()}
function feedbackClass(status){return status==='correct'?'correct':status==='accent'?'accent':'wrong'}
function safe(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function speak(text,rate=1){if(!('speechSynthesis'in window))return alert('Seu navegador não oferece síntese de voz.');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=dash?.preferences?.spanishVariant||'es';u.rate=rate;const voices=speechSynthesis.getVoices();const pref=(dash?.preferences?.spanishVariant||'es').toLowerCase();u.voice=voices.find(v=>v.lang?.toLowerCase()===pref)||voices.find(v=>v.lang?.toLowerCase().startsWith('es'))||null;speechSynthesis.speak(u)}
function xpToast(xp){$('#xpToast').textContent=`+${xp} XP`}
function explanationHtml(help){if(!help)return'';return `<details class="explain" open><summary>Entender o correto</summary><h4>${safe(help.title||'Explicação')}</h4><p>${safe(help.explanation||'')}</p>${help.example?`<p class="example">${safe(help.example)}</p>`:''}${help.tip?`<p><b>Como melhorar:</b> ${safe(help.tip)}</p>`:''}${help.recommendation?`<p><b>Como melhorar:</b> ${safe(help.recommendation)}</p>`:''}</details>`}

async function vocabulary(){showPractice('vocabulary','Recuperação ativa');const {item}=await api('/api/vocabulary/next');current=item;if(!item){$('#practiceCard').innerHTML='<p class="prompt">Tudo revisado por agora 🎯</p><p class="sub">Se houver muita revisão antiga, o SIDES pode ter pausado conteúdo novo para consolidar o que já foi estudado.</p>';return}
 $('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)} · ${safe(item.tags)}</span><p class="prompt">${safe(item.portuguese)}</p><p class="sub">Digite em espanhol antes de revelar.</p><input id="answer" autocomplete="off" autofocus><div class="actions"><button id="check">Verificar</button><button id="hear" class="ghost">Ouvir exemplo</button></div><div id="result"></div>`;
 $('#hear').onclick=()=>speak(item.example_es,.9);$('#check').onclick=()=>checkVocabulary();$('#answer').onkeydown=e=>{if(e.key==='Enter')checkVocabulary()};$('#answer').focus();}
async function checkVocabulary(){const answer=$('#answer').value;const prelim=await api('/api/vocabulary/check',{method:'POST',body:JSON.stringify({id:current.id,answer})});const result=$('#result');result.innerHTML=`<div class="feedback ${feedbackClass(prelim.quality.status)}"><b>${prelim.quality.status==='correct'?'Correto':prelim.quality.status==='accent'?'Quase: confira acentos':'Resposta esperada'}</b><p>${safe(prelim.expected)} — ${safe(prelim.translation)}</p><p>${safe(prelim.example)}</p></div>${explanationHtml(prelim.help)}<p class="sub">Agora avalie o esforço de recuperação, não apenas o acerto.</p><div class="actions ratings"><button class="again" data-r="1">Errei</button><button class="hard" data-r="2">Difícil</button><button class="good" data-r="3">Bom</button><button class="easy" data-r="4">Fácil</button></div>`;document.querySelectorAll('[data-r]').forEach(b=>b.onclick=async()=>{const data=await api('/api/vocabulary/review',{method:'POST',body:JSON.stringify({id:current.id,answer,rating:Number(b.dataset.r),responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);setTimeout(vocabulary,220)});$('#check').disabled=true;}

async function learning(kind='chunk',title=kind==='chunk'?'Frases e chunks':'Português × espanhol',skill=null){showPractice(kind,title);const qp=new URLSearchParams({kind});if(skill)qp.set('skill',skill);const {item}=await api(`/api/learning/next?${qp}`);current=item;if(!item){$('#practiceCard').innerHTML='<p class="prompt">Nada vencido neste foco agora.</p><p class="sub">O motor pode ter reduzido itens novos enquanto existem revisões antigas.</p>';return}$('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)} · ${safe(item.kind)}</span><p class="prompt">${safe(item.prompt)}</p><input id="answer" autocomplete="off" autofocus><button id="check">Responder</button><div id="result"></div>`;$('#check').onclick=()=>checkLearning(kind,title,skill);$('#answer').onkeydown=e=>{if(e.key==='Enter')checkLearning(kind,title,skill)};$('#answer').focus()}
async function checkLearning(kind,title,skill){const answer=$('#answer').value;const data=await api('/api/learning/answer',{method:'POST',body:JSON.stringify({id:current.id,kind,answer,responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);$('#result').innerHTML=`<div class="feedback ${feedbackClass(data.quality.status)}"><b>${data.quality.status==='correct'?'Correto':'Forma trabalhada'}</b><p>${safe(data.expected)}</p><p>${safe(data.example)}</p></div>${explanationHtml({title:data.guidance?.title||'Por que esta forma?',explanation:data.explanation,example:data.example,recommendation:data.guidance?.recommendation})}<div class="actions" style="margin-top:14px"><button id="next">Próximo</button></div>`;$('#next').onclick=()=>learning(kind,title,skill);$('#check').disabled=true}

async function grammar(skill=null){showPractice('grammar','Gramática em contexto');const qp=skill?`?skill=${encodeURIComponent(skill)}`:'';const {item}=await api(`/api/grammar/next${qp}`);current=item;$('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)} · ${safe(item.skill)}</span><p class="prompt">${safe(item.prompt)}</p><input id="answer" autocomplete="off" autofocus><button id="check">Responder</button><div id="result"></div>`;$('#check').onclick=()=>checkGrammar(skill);$('#answer').onkeydown=e=>{if(e.key==='Enter')checkGrammar(skill)};$('#answer').focus()}
async function checkGrammar(skill){const data=await api('/api/grammar/answer',{method:'POST',body:JSON.stringify({id:current.id,answer:$('#answer').value,responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);$('#result').innerHTML=`<div class="feedback ${feedbackClass(data.quality.status)}"><b>${data.quality.status==='correct'?'Correto':'Revise este ponto'}</b><p>Resposta: ${safe(data.expected.join(' / '))}</p><p>${safe(data.explanation)}</p></div>${explanationHtml(data.guidance)}<div class="actions" style="margin-top:14px"><button id="next">Próxima</button></div>`;$('#next').onclick=()=>grammar(skill);$('#check').disabled=true}

async function listening(){showPractice('listening','Ditado e compreensão oral');const {item}=await api('/api/listening/next');current=item;$('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)}</span><p class="prompt">Escute sem ler.</p><div class="actions"><button id="playSlow">0,75×</button><button id="play">1×</button><button id="playFast">1,15×</button></div><textarea id="answer" placeholder="Escreva o que você ouviu..."></textarea><button id="check">Verificar ditado</button><div id="result"></div>`;$('#playSlow').onclick=()=>speak(item.text,.75);$('#play').onclick=()=>speak(item.text,1);$('#playFast').onclick=()=>speak(item.text,1.15);$('#check').onclick=checkListening;setTimeout(()=>speak(item.text,.9),250)}
async function checkListening(){const data=await api('/api/listening/answer',{method:'POST',body:JSON.stringify({id:current.id,answer:$('#answer').value,responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);$('#result').innerHTML=`<div class="feedback ${feedbackClass(data.quality.status)}"><b>Texto ouvido</b><p>${safe(data.expected)}</p><p>${safe(data.translation)}</p></div>${explanationHtml(data.help)}<div class="actions" style="margin-top:14px"><button id="next">Próxima</button><button id="shadow" class="ghost">Ouvir e repetir</button></div>`;$('#next').onclick=listening;$('#shadow').onclick=()=>speak(data.expected,.82);$('#check').disabled=true}

async function reading(){showPractice('reading','Leitura com recuperação');const {item}=await api('/api/reading/next');current=item;$('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)}</span><h2>${safe(item.title)}</h2><div class="reading">${safe(item.body)}</div><div id="qs">${item.questions.map(q=>`<div class="question"><label>${safe(q.q)}</label><input data-q="${q.index}" autocomplete="off"></div>`).join('')}</div><button id="check">Corrigir</button><div id="result"></div>`;$('#check').onclick=checkReading}
async function checkReading(){const answers=[...document.querySelectorAll('[data-q]')].map(x=>x.value);const data=await api('/api/reading/answer',{method:'POST',body:JSON.stringify({id:current.id,answers,responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);$('#result').innerHTML=`<div class="feedback ${data.correct>=Math.ceil(data.total*.7)?'correct':'wrong'}"><b>${data.correct}/${data.total} respostas adequadas</b>${data.details.map(x=>`<p>${safe(x.q)}<br><small>Exemplo: ${safe(x.expected[0])}</small></p>`).join('')}</div>${explanationHtml(data.guidance)}<div class="actions" style="margin-top:14px"><button id="next">Próximo texto</button></div>`;$('#next').onclick=reading;$('#check').disabled=true}

async function speaking(){showPractice('speaking','Shadowing e produção oral');const {item}=await api('/api/listening/next');current=item;$('#practiceCard').innerHTML=`<span class="eyebrow">${safe(item.level)}</span><p class="prompt">${safe(item.text)}</p><p class="sub">1) Ouça. 2) Repita junto. 3) Grave sozinho. 4) Compare ritmo, clareza e entonação.</p><div class="actions"><button id="hear">Ouvir</button><button id="record" class="ghost">● Gravar</button><button id="stop" class="ghost" disabled>Parar</button><button id="done" disabled>Concluir repetição</button></div><div id="recordings"></div><p class="footerNote">A análise automática de inteligibilidade ficará no módulo local Whisper. O áudio não é enviado para serviços externos.</p>`;$('#hear').onclick=()=>speak(item.text,.82);$('#record').onclick=startRecording;$('#stop').onclick=stopRecording;$('#done').onclick=completeSpeaking}
async function startRecording(){if(!navigator.mediaDevices?.getUserMedia)return alert('Microfone não disponível neste navegador.');const stream=await navigator.mediaDevices.getUserMedia({audio:true});audioChunks=[];mediaRecorder=new MediaRecorder(stream);mediaRecorder.ondataavailable=e=>audioChunks.push(e.data);mediaRecorder.onstop=()=>{const blob=new Blob(audioChunks,{type:mediaRecorder.mimeType||'audio/webm'});if(recordingUrl)URL.revokeObjectURL(recordingUrl);recordingUrl=URL.createObjectURL(blob);$('#recordings').innerHTML=`<div class="feedback"><b>Sua gravação</b><audio controls src="${recordingUrl}"></audio><p>Compare com o modelo e repita até a fala ficar confortável.</p></div>`;if($('#done'))$('#done').disabled=false;stream.getTracks().forEach(t=>t.stop())};mediaRecorder.start();$('#record').disabled=true;$('#stop').disabled=false;$('#record').textContent='Gravando…'}
async function completeSpeaking(){const data=await api('/api/speaking/complete',{method:'POST',body:JSON.stringify({id:current.id,effort:3,responseMs:Math.round(performance.now()-startedAt)})});xpToast(data.xp);if($('#done'))$('#done').disabled=true;setTimeout(speaking,250)}
function stopRecording(){if(mediaRecorder&&mediaRecorder.state!=='inactive')mediaRecorder.stop();mediaRecorder=null;const r=$('#record'),s=$('#stop');if(r){r.disabled=false;r.textContent='● Gravar'}if(s)s.disabled=true}

async function placement(){showPractice('placement','Diagnóstico de nível');const {items}=await api('/api/placement');current=items;$('#practiceCard').innerHTML=`<p class="sub">Sem consulta. Marque a resposta que parece natural. O objetivo é calibrar, não obter uma nota.</p><div class="sessionList">${items.map((x,i)=>`<div class="sessionItem"><b>${i+1}. ${safe(x.prompt)}</b><div class="options">${x.options.map(o=>`<label><input type="radio" name="p${x.id}" value="${safe(o)}"> ${safe(o)}</label>`).join('')}</div></div>`).join('')}</div><button id="finish">Calibrar nível</button><div id="result"></div>`;$('#finish').onclick=submitPlacement}
async function submitPlacement(){const answers={};for(const x of current){const selected=document.querySelector(`input[name=p${x.id}]:checked`);if(selected)answers[x.id]=selected.value}const data=await api('/api/placement',{method:'POST',body:JSON.stringify({answers})});xpToast(data.xp);$('#result').innerHTML=`<div class="feedback correct"><b>Nível inicial estimado: ${safe(data.level)}</b><p>${data.correct}/${data.total} itens corretos. Esse nível é apenas o ponto de partida; o desempenho nas sessões ajustará a dificuldade.</p></div><div class="actions" style="margin-top:14px"><button id="go">Ir para o painel</button></div>`;$('#go').onclick=back;$('#finish').disabled=true}

async function session(){showPractice('session','Sessão diária intercalada');const {items}=await api('/api/session?limit=20');$('#practiceCard').innerHTML=`<p class="prompt">Sua sessão tem ${items.length} blocos.</p><p class="sub">A ordem intercala habilidades e prioriza erros, pontos fracos e revisões vencidas.</p><div class="sessionList">${items.map((x,i)=>`<div class="sessionItem"><b>${i+1}. ${label(x.kind)}</b> · ${safe(x.level||'')}</div>`).join('')}</div><div class="actions" style="margin-top:18px"><button id="begin">Começar pelo primeiro foco</button></div>`;$('#begin').onclick=()=>startKind(items[0]?.kind||'vocabulary')}
function startKind(kind){({vocabulary,grammar,listening,reading,chunk:()=>learning('chunk'),contrast:()=>learning('contrast')}[kind]||vocabulary)()}
function focusPractice(type,key){if(type==='grammar')return grammar(key);if(type==='chunk'||type==='contrast')return learning(type,type==='chunk'?'Frases e chunks':'Português × espanhol',key);if(type==='listening')return listening();if(type==='reading')return reading();if(type==='speaking')return speaking();if(type==='jw'){location.href='/jw.html';return}return vocabulary()}

document.addEventListener('click',e=>{const focus=e.target.closest('[data-focus-type]');if(focus){focusPractice(focus.dataset.focusType,focus.dataset.focusKey);return}const v=e.target.closest('[data-view]')?.dataset.view;if(!v)return;({vocabulary,chunk:()=>learning('chunk'),contrast:()=>learning('contrast'),grammar,listening,reading,speaking,placement}[v]||(()=>{}))()});
$('#backBtn').onclick=back;$('#startSession').onclick=session;$('#exportBtn').onclick=()=>location.href='/api/export';
refresh().catch(err=>{$('#heroText').textContent=`Falha ao carregar o painel: ${err.message}`});
